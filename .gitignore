.env
SettingsEnv.env

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# iOS
ios/App/Pods/
*.xcworkspace
*.xcuserstate
DerivedData/

# Firebase client configs are committed so CI can build (not secrets — they ship in the app binary).
# The service-account key below IS a real secret and stays ignored.
firebase-service-account.json

# Local scratch: raw SQL result dumps (contain live student data)
one.txt
